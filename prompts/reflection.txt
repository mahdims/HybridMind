You are critically evaluating your own algorithm proposal.

Your Current Proposal:
{current_idea}

Task: Perform critical self-reflection and improve your algorithm.

Evaluate these aspects:
1. **Correctness**: Are there any logical flaws, bugs, or edge cases not properly handled?
   - Check boundary conditions
   - Verify algorithm logic
   - Test against corner cases

2. **Efficiency**: Can the time or space complexity be improved?
   - Identify bottlenecks
   - Consider alternative data structures
   - Look for redundant operations

3. **Clarity**: Is the explanation clear, well-structured, and complete?
   - Check if steps are clearly defined
   - Verify pseudocode correctness
   - Ensure complexity analysis is accurate

4. **Completeness**: Are there missing details, unstated assumptions, or gaps?
   - List all assumptions explicitly
   - Handle all input types
   - Consider scalability

================================================================================
OUTPUT FORMAT - FOLLOW EXACTLY (MANDATORY)
================================================================================

You MUST provide your response in this EXACT two-section format:

---FLAWS---
[List 2-3 specific weaknesses, flaws, or gaps in the current proposal. Be concise and specific. This section is for analysis only.]

---REVISED_ALGORITHM---
[Provide the COMPLETE improved version of the algorithm that addresses the identified flaws. This should be a self-contained, standalone algorithm description that can be understood without reading the flaws section. Include all necessary details: approach, pseudocode, complexity analysis, and explanations.]

CRITICAL REQUIREMENTS:
1. Start flaws section with EXACTLY "---FLAWS---" on its own line
2. Start revised algorithm section with EXACTLY "---REVISED_ALGORITHM---" on its own line
3. The revised algorithm section must be COMPLETE and STANDALONE
4. Do NOT reference the flaws section in the revised algorithm
5. Do NOT say "as mentioned above" or "to address the issues" in the revised algorithm
6. The revised algorithm should be a polished, final version ready for evaluation
7. Focus on substantive improvements, not just rewording

EXAMPLE FORMAT:

---FLAWS---
1. The current approach doesn't handle negative numbers in the input array, which could lead to incorrect results.
2. The time complexity analysis assumes uniform distribution, but worst-case scenarios aren't addressed.
3. The pseudocode lacks clear handling of the base case when the array is empty.

---REVISED_ALGORITHM---
**Enhanced Binary Search with Comprehensive Edge Case Handling**

**Approach:**
This algorithm implements an optimized binary search that handles all edge cases including negative numbers, empty arrays, and duplicate values...

[Complete algorithm description follows...]

**Pseudocode:**
```
function binarySearch(arr, target):
    if arr is empty:
        return -1
    ...
```

**Time Complexity:** O(log n) in all cases...
**Space Complexity:** O(1)...

================================================================================
